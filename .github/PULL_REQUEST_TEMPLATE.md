## Qué cambia

<!-- feat/fix/docs + alcance -->

## Checklist

- [ ] `npm run typecheck:all && npm run lint && npm run format:check`
- [ ] `npm run build && npm test && npm run verify:fixtures`
- [ ] `validate` + `normalize --check` del golden en verde
- [ ] Docs actualizadas (`README.md` y `DECISIONS.md` si toca `schemas/`, códigos o comandos)
- [ ] Sin artefactos (`dist/`, `coverage/`, `*.tar.gz`)
